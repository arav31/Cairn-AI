{
  "name": "cairn",
  "version": "0.1.0",
  "private": true,
  "description": "Cairn pilot demo: compile authorized browser workflows into verified, permissioned APIs.",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "db:migrate": "node scripts/migrate-database.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
